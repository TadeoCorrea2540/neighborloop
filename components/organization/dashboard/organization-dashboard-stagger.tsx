"use client";

import type { ReactNode } from "react";

export default function OrganizationDashboardStagger({ children }: { children: ReactNode }) {
  return <div className="org-dash-stagger">{children}</div>;
}
